'use client';
'use strict';

var jsxRuntime = require('react/jsx-runtime');
var react = require('react');
var useLiveQuery = require('./useLiveQuery.cjs');
function LiveQueryClientComponent(props) {
  const {
    initialData,
    query,
    params,
    children
  } = props;
  const data = useLiveQuery.useLiveQuery(initialData, query, params);
  return /* @__PURE__ */jsxRuntime.jsx(Slot, {
    data,
    children
  });
}
LiveQueryClientComponent.displayName = "LiveQuery";
const Slot = props => {
  const {
    children,
    data,
    ...slotProps
  } = props;
  if (react.isValidElement(children)) {
    return react.cloneElement(children, {
      ...slotProps,
      ...children.props,
      // all child props should override, except for `data`
      data,
      // eslint-disable-next-line no-warning-comments
      // @ts-expect-error -- @todo fix the typings
      ref: children.ref
    });
  }
  return react.Children.count(children) > 1 ? react.Children.only(null) : null;
};
Slot.displayName = "Slot";
module.exports = LiveQueryClientComponent;
//# sourceMappingURL=LiveQueryClientComponent.cjs.map
